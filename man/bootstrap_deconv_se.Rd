% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admm_solver.R
\name{bootstrap_deconv_se}
\alias{bootstrap_deconv_se}
\title{Parametric Bootstrap Standard Errors for Deconvolution}
\usage{
bootstrap_deconv_se(
  Y,
  SE_Y,
  M,
  W = NULL,
  D,
  Ls,
  lambda_t,
  lambda_s,
  rho = 1,
  ridge = 1e-08,
  max_iter = 1000L,
  tol_abs = 1e-04,
  tol_rel = 0.001,
  B = 200L,
  conf_level = 0.9,
  clip = NULL,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{Y}{n x S numeric matrix of observed smoothed proportions /
counts.}

\item{SE_Y}{n x S numeric matrix of ACS standard errors for \code{Y}
(same dimension; \code{NA} entries are treated as zero perturbation).}

\item{M}{S x T convolution matrix.}

\item{W}{(nS) x (nS) sparse diagonal precision matrix from
\code{\link[=build_precision_weights]{build_precision_weights()}}, or \code{NULL} for unweighted LS.}

\item{D}{(T-k) x T temporal difference matrix from
\code{\link[=build_temporal_penalty]{build_temporal_penalty()}}.}

\item{Ls}{n x n spatial Laplacian from \code{\link[=build_spatial_laplacian]{build_spatial_laplacian()}}.}

\item{lambda_t}{Non-negative temporal regularisation strength.}

\item{lambda_s}{Non-negative spatial regularisation strength.}

\item{rho}{ADMM augmented-Lagrangian penalty parameter (default \code{1}).}

\item{ridge}{Ridge for system matrix PD guarantee (default \code{1e-8}).}

\item{max_iter}{Maximum ADMM iterations per replicate (default \code{1000}).}

\item{tol_abs}{Absolute convergence tolerance (default \code{1e-4}).}

\item{tol_rel}{Relative convergence tolerance (default \code{1e-3}).}

\item{B}{Number of bootstrap replicates (default \code{200}).}

\item{conf_level}{Confidence level for MOE (default \code{0.90}).}

\item{clip}{Length-2 numeric vector \code{c(lo, hi)} to clip bootstrap
draws of \code{Y} before solving (e.g. \code{c(0, 1)} for proportions).  \code{NULL}
disables clipping (default).}

\item{seed}{Integer random seed passed to \code{\link[=set.seed]{set.seed()}} for
reproducibility.  \code{NULL} uses the current RNG state (default).}

\item{verbose}{Logical; if \code{TRUE} prints replicate progress (default
\code{FALSE}).}
}
\value{
An object of class \code{"DeconvBootstrap"}, a list with elements:
\describe{
\item{\code{X_hat}}{n x T point-estimate matrix (from the original \code{Y}).}
\item{\code{SE}}{n x T empirical bootstrap standard errors.}
\item{\code{MOE}}{n x T margins of error at the requested \code{conf_level}.}
\item{\code{CI_low}}{n x T lower confidence bound (\code{X_hat - MOE}).}
\item{\code{CI_high}}{n x T upper confidence bound (\code{X_hat + MOE}).}
\item{\code{B}}{Number of replicates used.}
\item{\code{conf_level}}{Confidence level.}
}
}
\description{
Estimates uncertainty in the recovered latent annual signal \eqn{\hat{X}}
by perturbing the observed data matrix \eqn{Y} with its ACS sampling
standard errors and re-solving the deconvolution \eqn{B} times.  The
expensive sparse Cholesky factorisation of the system matrix is computed
\strong{once} and reused across all bootstrap replicates via \link{ADMMSolver}\verb{$reset_y()}.
}
\seealso{
\code{\link[=solve_deconv]{solve_deconv()}}, \link{ADMMSolver}, \code{\link[=build_precision_weights]{build_precision_weights()}}
}
