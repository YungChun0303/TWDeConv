% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty_matrices.R
\name{build_empirical_laplacian}
\alias{build_empirical_laplacian}
\title{Build the Empirical Spatial Graph Laplacian from ACS Census Tract Geometries}
\usage{
build_empirical_laplacian(acs_sf_data, queen = TRUE)
}
\arguments{
\item{acs_sf_data}{An \code{\link[sf:sf]{sf}} object of Census tract polygons.
Typically the \verb{$sf} component of an \code{ACSData} object returned by
\code{\link[=fetch_acs_vre_data]{fetch_acs_vre_data()}}. Should contain a \code{GEOID} column; if absent,
row names fall back to \code{"tract_1"}, \code{"tract_2"}, ....}

\item{queen}{Logical. Use Queen contiguity (shared edge or vertex)?
Default \code{TRUE}. Set to \code{FALSE} for Rook contiguity (shared edge only).}
}
\value{
A sparse symmetric matrix of class
\code{\link[Matrix:dsCMatrix-class]{dsCMatrix}} with dimensions
\eqn{n \times n} where \eqn{n} is the number of tracts. Row and column
names match the \code{GEOID} values of \code{acs_sf_data}.
}
\description{
Given an \code{\link[sf:sf]{sf}} object of Census tract polygon geometries (typically
the \verb{$sf} component of an \code{ACSData} object returned by
\code{\link[=fetch_acs_vre_data]{fetch_acs_vre_data()}}), computes the Queen contiguity graph among tracts
using \code{\link[spdep:poly2nb]{spdep::poly2nb()}} and returns the combinatorial graph Laplacian
\deqn{L_s = D_s - A,}
where \eqn{A} is the binary adjacency matrix and
\eqn{D_s = \operatorname{diag}(A \mathbf{1})} is the degree matrix.

Row and column names of the returned matrix are set to the \code{GEOID} column
of \code{acs_sf_data}, so \eqn{L_s} aligns directly with the weight matrix
\eqn{W} produced by \code{\link[=build_acs_precision_weights]{build_acs_precision_weights()}}.
}
\details{
\strong{Contiguity rule.} \code{queen = TRUE} (default) applies Queen contiguity
(shared edge \emph{or} vertex), which is recommended for irregular Census tract
polygons to avoid spurious disconnections at near-touching boundaries.

\strong{Properties of \eqn{L_s}.} \eqn{L_s} is symmetric positive semi-definite.
The penalty \eqn{x^\top L_s x = \sum_{(i,j)\in\mathcal{E}}(x_i-x_j)^2}
discourages spatially non-smooth deconvolution solutions.

\strong{Island tracts.} Tracts with no contiguous neighbours receive a zero
row/column in \eqn{L_s} and trigger a warning. They are not penalised
spatially but remain in the deconvolution system.
}
\examples{
\dontrun{
acs <- fetch_acs_vre_data(
  state     = "IL",
  county    = "Cook",
  year_end  = 2022,
  variables = c(med_inc = "B19013_001")
)

Ls <- build_empirical_laplacian(acs$sf)
dim(Ls)   # n_tracts x n_tracts

# Laplacian property: row sums are zero
all(abs(Matrix::rowSums(Ls)) < 1e-12)

# Plug into ADMM solver alongside W
W <- build_acs_precision_weights(acs)
}

}
\seealso{
\code{\link[=fetch_acs_vre_data]{fetch_acs_vre_data()}}, \code{\link[=build_acs_precision_weights]{build_acs_precision_weights()}},
\code{\link[=build_spatial_laplacian]{build_spatial_laplacian()}}, \code{\link[spdep:poly2nb]{spdep::poly2nb()}}
}
