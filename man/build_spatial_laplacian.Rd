% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty_matrices.R
\name{build_spatial_laplacian}
\alias{build_spatial_laplacian}
\title{Build the spatial graph Laplacian from an sf grid}
\usage{
build_spatial_laplacian(sf_obj, queen = TRUE)
}
\arguments{
\item{sf_obj}{An \code{\link[sf:sf]{sf}} object whose geometries are polygons.
Typically the \verb{$grid} field from a \link{SyntheticDataGenerator} instance.}

\item{queen}{Logical. Use queen contiguity? Default \code{TRUE}.}
}
\value{
A sparse symmetric matrix of class
\code{\link[Matrix:dsCMatrix-class]{dsCMatrix}} with dimensions
\eqn{n \times n} (one row/column per cell).  Row and column names
match \code{sf_obj$cell_id} when present, otherwise \code{"cell_1"}, ....
}
\description{
Given an \code{\link[sf:sf]{sf}} object of polygon geometries, computes the
contiguity graph among cells using \code{\link[spdep:poly2nb]{spdep::poly2nb()}} and returns the
combinatorial graph Laplacian
\deqn{L_s = D_s - A,}
where \eqn{A} is the binary adjacency matrix and
\eqn{D_s = \operatorname{diag}(A \mathbf{1})} is the degree matrix.
}
\details{
\strong{Contiguity rule.}  \code{queen = TRUE} (default) uses queen contiguity
(shared edge \emph{or} vertex), giving up to 8 neighbours on a regular grid.
\code{queen = FALSE} uses rook contiguity (shared edge only), giving up to 4
neighbours -- the standard discrete Laplacian on a lattice.

\strong{Properties of \eqn{L_s}.}
\eqn{L_s} is symmetric positive semi-definite.  Its null space is spanned
by the constant vector, so the smallest eigenvalue is 0 (one per connected
component). The penalty \eqn{x^\top L_s x = \sum_{(i,j) \in \mathcal{E}}
(x_i - x_j)^2} discourages spatially non-smooth solutions.

\strong{Isolated cells.} Cells with no neighbours (e.g., non-contiguous islands
in a cropped grid) receive a zero row/column and are flagged with a warning.
}
\examples{
\dontrun{
gen <- SyntheticDataGenerator$new(n_row = 5, n_col = 4)
gen$generate_grid()

Ls <- build_spatial_laplacian(gen$grid, queen = FALSE)
dim(Ls)       # 20 x 20
# Laplacian property: row sums are zero
all(abs(Matrix::rowSums(Ls)) < 1e-12)
}

}
