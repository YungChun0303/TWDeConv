% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acs_weights.R
\name{build_acs_precision_weights}
\alias{build_acs_precision_weights}
\title{Build a Precision Weight Matrix from an ACSData Object}
\usage{
build_acs_precision_weights(
  acs_data,
  variables = NULL,
  use_srvyr = TRUE,
  zero_var_action = c("max_weight", "drop", "error"),
  quiet = FALSE
)
}
\arguments{
\item{acs_data}{An object of class \code{"ACSData"} returned by
\code{\link[=fetch_acs_vre_data]{fetch_acs_vre_data()}}.}

\item{variables}{Character vector of variable names (must match
\code{acs_data$metadata$var_names}). \code{NULL} (default) uses all
variables.}

\item{use_srvyr}{Logical. If \code{TRUE} (default), use
\code{srvyr::as_survey_rep()} when both \strong{srvyr} / \strong{survey} are
installed and VRE replicates are available.  Set to \code{FALSE} to
always use the pre-computed SE from \code{acs_data$se}.}

\item{zero_var_action}{Character. How to handle zero or non-finite
variance: \code{"max_weight"} (default) caps precision at the maximum
observed value; \code{"drop"} sets precision to 0 (observation
excluded from WLS); \code{"error"} stops with an error message.}

\item{quiet}{Logical. Suppress progress messages (default \code{FALSE}).}
}
\value{
A sparse diagonal matrix of class
\code{\link[Matrix:ddiMatrix-class]{ddiMatrix}} with dimension
\eqn{(n \cdot p) \times (n \cdot p)}, where \eqn{n} is the
number of tracts and \eqn{p} the number of selected variables.
Row/column names are \code{"GEOID:variable"} in survey-within-cell
order.  Pass directly to \code{\link[=solve_deconv]{solve_deconv()}} as the \code{W} argument.
}
\description{
Converts ACS 5-year standard errors (either from the 80 Variance
Replicate Estimates via SDR or from the published margin of error)
into the diagonal inverse-variance weight matrix \eqn{W} used in the
Weighted Least Squares deconvolution objective.

When the \strong{srvyr} and \strong{survey} packages are installed and the
\code{ACSData} object contains VRE replicate columns
(\code{vre_source == "sdr"}), the function constructs a formal
\emph{successive-difference replicate} survey design via
\code{srvyr::as_survey_rep()} and computes per-tract variances using
domain estimation (\code{survey::svyby()}).
Otherwise it uses the pre-computed SE already stored in
\code{acs_data$se} (which is the SDR SE when replicates were available
or MOE-derived SE otherwise).
}
\details{
\subsection{Survey replicate design}{

For each selected variable the function builds an \eqn{n}-row data
frame (one row per tract) and creates a replicate design with
80 replicate weight columns equal to
\eqn{\hat{\theta}_{r,i} / \hat{\theta}_i}.
With base weight 1, the domain total in replicate \eqn{r} for
tract \eqn{i} is exactly \eqn{\hat{\theta}_{r,i}}, so
\code{survey::svyby(~estimate, ~GEOID, design, svytotal)} returns the
SDR standard error
\eqn{\sqrt{4/80 \cdot \sum_r (\hat{\theta}_{r,i} -
\hat{\theta}_i)^2}}.

Tracts with a zero or missing base estimate cannot form a replicate
ratio and fall back to MOE-derived SE.
}

\subsection{Output ordering}{

The diagonal of \eqn{W} follows \strong{survey-within-cell} ordering,
matching the ADMM \eqn{y}-vector convention
(\eqn{y = \operatorname{vec}(Y^\top)}):
\deqn{W_{(i-1)p+j,\,(i-1)p+j} = 1 / \hat{V}(\hat{\theta}_{i,j})}
where \eqn{i} indexes tracts and \eqn{j} indexes variables.
}
}
\examples{
\dontrun{
acs <- fetch_acs_vre_data(
  state     = "IL",
  county    = "Cook",
  year_end  = 2022,
  variables = c(med_inc = "B19013_001"),
  vre       = TRUE
)

W <- build_acs_precision_weights(acs)
dim(W)   # n_tracts  x  n_tracts (one variable)

# Plug into deconvolution pipeline
Ls <- build_spatial_laplacian(acs$sf)
D  <- build_temporal_penalty(T = 10, k = 2)
# (stack Y across ACS windows first -- see fetch_acs_vre_data docs)
}

}
\seealso{
\code{\link[=fetch_acs_vre_data]{fetch_acs_vre_data()}}, \code{\link[=build_precision_weights]{build_precision_weights()}},
\code{\link[=compute_sdr_se]{compute_sdr_se()}}, \code{\link[=solve_deconv]{solve_deconv()}}
}
