% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_suite.R
\name{run_simulation_suite}
\alias{run_simulation_suite}
\title{Run a Hyperparameter Grid Search for Spatio-Temporal Deconvolution}
\usage{
run_simulation_suite(
  gen,
  lambda_t_grid,
  lambda_s_grid,
  D,
  Ls,
  W = NULL,
  rho = 1,
  ridge = 1e-08,
  max_iter = 1000L,
  tol_abs = 1e-04,
  tol_rel = 0.001,
  keep_best = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{gen}{A \link{SyntheticDataGenerator} object that has already been
simulated (i.e., \verb{$simulate()} called).}

\item{lambda_t_grid}{Numeric vector of \eqn{\lambda_t} values to try.}

\item{lambda_s_grid}{Numeric vector of \eqn{\lambda_s} values to try.}

\item{D}{(T-k)  x  T temporal difference matrix from
\code{\link[=build_temporal_penalty]{build_temporal_penalty()}}.}

\item{Ls}{n  x  n spatial Laplacian from \code{\link[=build_spatial_laplacian]{build_spatial_laplacian()}}.}

\item{W}{(nS)  x  (nS) diagonal precision matrix from
\code{\link[=build_precision_weights]{build_precision_weights()}}.  Pass \code{NULL} for unweighted LS.}

\item{rho}{Positive ADMM penalty parameter (default \code{1}).}

\item{ridge}{Small ridge for numerical stability (default \code{1e-8}).}

\item{max_iter}{Maximum ADMM iterations per grid point (default \code{1000}).}

\item{tol_abs, tol_rel}{Convergence tolerances (defaults \code{1e-4}, \code{1e-3}).}

\item{keep_best}{Logical.  If \code{TRUE} (default), store \verb{$X_hat} for the
grid point achieving the lowest RMSE.}

\item{verbose}{Logical.  Print per-combination progress (default \code{FALSE}).}
}
\value{
An S3 object of class \code{"SimulationSuite"} -- a named list with:
\describe{
\item{\verb{$results}}{\code{data.frame} with columns \code{lambda_t}, \code{lambda_s},
\code{rmse}, \code{ccc}, \code{converged}, \code{n_iter}.}
\item{\verb{$best}}{Named list: \code{lambda_t}, \code{lambda_s}, \code{rmse}, \code{ccc},
\code{X_hat} for the best (lowest-RMSE) grid point.  \code{NULL} if
\code{keep_best = FALSE}.}
\item{\verb{$detail}}{Named list with \verb{$cell} (per-cell metrics for the
best run) and \verb{$year} (per-year metrics for the best run).}
\item{\verb{$timing_sec}}{Wall-clock seconds for setup and total sweep.}
\item{\verb{$params}}{Copy of key input parameters for reproducibility.}
}
}
\description{
Sweeps a grid of \eqn{(\lambda_t, \lambda_s)} values, fits the ADMM
solver for each combination, and returns RMSE and Lin's Concordance
Correlation Coefficient (CCC) against the known ground-truth latent
signal.
}
\details{
\subsection{Efficiency}{

The ADMM system matrix
\deqn{A = \tilde{M}^\top W \tilde{M}
          + \rho (D_t^\top D_t + D_s^\top D_s) + \delta I}
does \strong{not} depend on \eqn{\lambda_t} or \eqn{\lambda_s}.  Therefore
\code{run_simulation_suite()} calls \link{ADMMSolver}\verb{$setup()} \strong{once} (one
sparse Cholesky factorisation) and then re-runs the ADMM loop for each
grid point, reusing the factorisation.  This makes grid searches
substantially faster than naive repeated calls to \code{\link[=solve_deconv]{solve_deconv()}}.
}

\subsection{Metrics}{
\itemize{
\item \strong{RMSE} -- root mean squared error over all \eqn{n \times T} elements.
\item \strong{CCC} -- Lin's (1989) concordance correlation coefficient, which
simultaneously penalises bias (location shift), scale differences,
and correlation:
\deqn{\rho_c = \frac{2\,\text{Cov}(X, \hat{X})}
        {\text{Var}(X) + \text{Var}(\hat{X}) +
         (\bar{X} - \bar{\hat{X}})^2}.}
Ranges in \eqn{[-1, 1]}; perfect recovery gives \eqn{\rho_c = 1}.
}

Per-cell and per-year breakdowns of each metric are included in the
returned \verb{$detail} list.
}
}
\examples{
\dontrun{
gen <- SyntheticDataGenerator$new(n_row=6, n_col=6, seed=1L)
gen$simulate()
D  <- build_temporal_penalty(gen$params$T_years, k = 2)
Ls <- build_spatial_laplacian(gen$grid, queen = FALSE)
W  <- build_precision_weights(matrix(0.4, 36, 16))

suite <- run_simulation_suite(
  gen           = gen,
  lambda_t_grid = c(0.01, 0.05, 0.1, 0.5),
  lambda_s_grid = c(0.01, 0.05, 0.1),
  D = D, Ls = Ls, W = W,
  verbose = TRUE
)
print(suite)
plot_lambda_heatmap(suite)
plot_cell_signals(gen, suite$best$X_hat)
}

}
