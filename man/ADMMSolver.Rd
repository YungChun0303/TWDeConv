% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admm_solver.R
\name{ADMMSolver}
\alias{ADMMSolver}
\title{ADMM Solver for the Spatio-Temporal Generalised Lasso}
\description{
Solves the Kronecker-structured generalised lasso problem

\deqn{\min_X \;
  \tfrac{1}{2} \| W^{1/2} \bigl(\operatorname{vec}(Y)
      - (I_n \otimes M)\operatorname{vec}(X^\top)\bigr) \|_2^2
  + \lambda_t \|(I_n \otimes D)\operatorname{vec}(X^\top)\|_1
  + \lambda_s \|(L_s \otimes I_T)\operatorname{vec}(X^\top)\|_1}

via the Alternating Direction Method of Multipliers (ADMM).
}
\details{
\subsection{Algorithm (scaled dual form, Boyd et al. 2010, Sec. 3.1.1)}{

Introducing auxiliary variables \eqn{z_t = D_t x} and \eqn{z_s = D_s x},
the augmented Lagrangian is split into three closed-form steps per
iteration:

\describe{
\item{x-update}{Solve one positive-definite linear system
\eqn{A x^{k+1} = b^k} where
\deqn{A = \tilde{M}^\top W \tilde{M}
              + \rho (D_t^\top D_t + D_s^\top D_s) + \delta I}
is \strong{precomputed and Cholesky-factorised once} (before the loop).
Only the right-hand side \eqn{b^k} changes each iteration.}
\item{z-updates}{Element-wise soft-thresholding (proximal operator
of the \eqn{\ell_1} norm):
\eqn{z_t^{k+1} = S_{\lambda_t/\rho}(D_t x^{k+1} + u_t^k)},
similarly for \eqn{z_s}.}
\item{u-updates}{Dual variable accumulation:
\eqn{u_t^{k+1} = u_t^k + D_t x^{k+1} - z_t^{k+1}}.}
}
}

\subsection{Efficiency note}{

Because \eqn{A} does \strong{not} depend on \eqn{\lambda_t} or
\eqn{\lambda_s}, a single call to \verb{$setup()} (which factors \eqn{A})
supports a full grid search over \eqn{(\lambda_t, \lambda_s)} by
re-running \verb{$run()} only -- no re-factorisation needed.
}

\subsection{Convergence criterion}{

Stopping uses the primal and dual residual norms (Boyd et al. 2010,
Sec. 3.3.1) with absolute tolerance \code{tol_abs} and relative tolerance
\code{tol_rel}.
}
}
\examples{
\dontrun{
gen <- SyntheticDataGenerator$new(n_row=6, n_col=6, seed=1L)
gen$simulate()
D  <- build_temporal_penalty(gen$params$T_years, k = 2)
Ls <- build_spatial_laplacian(gen$grid, queen = FALSE)
W  <- build_precision_weights(matrix(0.5, 36, 16))

solver <- ADMMSolver$new(gen$Y, gen$M, W, D, Ls,
                         lambda_t = 0.1, lambda_s = 0.05)
solver$setup()
solver$run(verbose = TRUE)
solver$print()
dim(solver$X_hat)   # 36 x 20
solver$compute_objective()
}

}
\references{
Boyd, S., Parikh, N., Chu, E., Peleato, B., & Eckstein, J. (2010).
\emph{Distributed Optimization and Statistical Learning via the Alternating
Direction Method of Multipliers.} Foundations and Trends in Machine
Learning, 3(1), 1-122.
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{Y}}{n  x  S matrix of observed smoothed data.}

\item{\code{M}}{S  x  T convolution matrix (sparse).}

\item{\code{W}}{(nS)  x  (nS) sparse diagonal precision matrix.}

\item{\code{D}}{(T-k)  x  T temporal difference operator (sparse).}

\item{\code{Ls}}{n  x  n spatial graph Laplacian (sparse, symmetric).}

\item{\code{lambda_t}}{Temporal regularisation parameter >= 0.}

\item{\code{lambda_s}}{Spatial regularisation parameter >= 0.}

\item{\code{rho}}{ADMM augmented-Lagrangian penalty parameter > 0.}

\item{\code{ridge}}{Ridge added to system matrix diagonal for PD guarantee.}

\item{\code{n_cells}}{Number of spatial cells (n).}

\item{\code{T_years}}{Number of latent annual time points (T).}

\item{\code{n_periods}}{Number of observed smoothed periods (S = ncol(Y)).}

\item{\code{M_kron}}{(nS)  x  (nT) sparse matrix \eqn{I_n \otimes M}.}

\item{\code{Dt_kron}}{n(T-k)  x  (nT) sparse matrix \eqn{I_n \otimes D}.}

\item{\code{Ds_kron}}{(nT)  x  (nT) sparse matrix \eqn{L_s \otimes I_T}.}

\item{\code{A_sys}}{(nT)  x  (nT) sparse SPD system matrix.}

\item{\code{X_hat}}{n  x  T matrix of recovered latent annual signals.}

\item{\code{history}}{\code{data.frame} with columns \code{iter}, \code{r_primal}, \code{r_dual}.}

\item{\code{converged}}{Logical; TRUE if stopping criteria were satisfied.}

\item{\code{.chol_A}}{CHMfactor (sparse Cholesky of \code{A_sys}). Populated by
\verb{$setup()}.}

\item{\code{.q_data}}{nT-vector \eqn{M^\top W y} (fixed data term in RHS).
Populated by \verb{$setup()}.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-ADMMSolver-new}{\code{ADMMSolver$new()}}
\item \href{#method-ADMMSolver-setup}{\code{ADMMSolver$setup()}}
\item \href{#method-ADMMSolver-run}{\code{ADMMSolver$run()}}
\item \href{#method-ADMMSolver-compute_objective}{\code{ADMMSolver$compute_objective()}}
\item \href{#method-ADMMSolver-print}{\code{ADMMSolver$print()}}
\item \href{#method-ADMMSolver-clone}{\code{ADMMSolver$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ADMMSolver-new"></a>}}
\if{latex}{\out{\hypertarget{method-ADMMSolver-new}{}}}
\subsection{Method \code{new()}}{
Create a new \code{ADMMSolver}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ADMMSolver$new(
  Y,
  M,
  W = NULL,
  D,
  Ls,
  lambda_t,
  lambda_s,
  rho = 1,
  ridge = 1e-08
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{Y}}{n  x  S numeric matrix of observed data.}

\item{\code{M}}{S  x  T convolution matrix.}

\item{\code{W}}{(nS)  x  (nS) sparse diagonal precision matrix;
\code{NULL} for unweighted LS; a positive scalar for homoscedastic LS.}

\item{\code{D}}{(T-k)  x  T temporal difference matrix from
\code{\link[=build_temporal_penalty]{build_temporal_penalty()}}.}

\item{\code{Ls}}{n  x  n spatial Laplacian from
\code{\link[=build_spatial_laplacian]{build_spatial_laplacian()}}.}

\item{\code{lambda_t}}{Non-negative scalar temporal regularisation strength.}

\item{\code{lambda_s}}{Non-negative scalar spatial regularisation strength.}

\item{\code{rho}}{ADMM penalty parameter (default 1).  A good rule of
thumb is \verb{rho ~= sqrt(lambda_t * lambda_s)} or \code{rho = 1} to start.}

\item{\code{ridge}}{Small positive scalar added to diag(A_sys) to
guarantee strict positive definiteness (default 1e-8).}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ADMMSolver-setup"></a>}}
\if{latex}{\out{\hypertarget{method-ADMMSolver-setup}{}}}
\subsection{Method \code{setup()}}{
Precompute Kronecker operators, assemble the system matrix \eqn{A},
and compute its sparse Cholesky factorisation.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ADMMSolver$setup()}\if{html}{\out{</div>}}
}

\subsection{Details}{
\eqn{A} is assembled as
\deqn{A = \tilde{M}^\top W \tilde{M}
          + \rho (D_t^\top D_t + D_s^\top D_s) + \delta I,}
where \eqn{\delta} = \code{ridge}.  Because \eqn{A} is independent of
\eqn{\lambda_t} and \eqn{\lambda_s}, this factorisation can be
reused for any regularisation grid.
}

\subsection{Returns}{
\code{self} invisibly.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ADMMSolver-run"></a>}}
\if{latex}{\out{\hypertarget{method-ADMMSolver-run}{}}}
\subsection{Method \code{run()}}{
Execute the ADMM iterations.  Calls \code{setup()} automatically if not
already invoked.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ADMMSolver$run(
  max_iter = 1000L,
  tol_abs = 1e-04,
  tol_rel = 0.001,
  verbose = FALSE,
  warm_start = FALSE
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{max_iter}}{Positive integer. Maximum iterations (default 1000).}

\item{\code{tol_abs}}{Absolute convergence tolerance (default 1e-4).}

\item{\code{tol_rel}}{Relative convergence tolerance (default 1e-3).}

\item{\code{verbose}}{Logical. Print progress every 100 iterations
(default \code{FALSE}).}

\item{\code{warm_start}}{Logical. If \code{TRUE} and \verb{$X_hat} already exists,
initialise \code{x} from the previous solution (default \code{FALSE}).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
\code{self} invisibly.  Results stored in \verb{$X_hat} and \verb{$history}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ADMMSolver-compute_objective"></a>}}
\if{latex}{\out{\hypertarget{method-ADMMSolver-compute_objective}{}}}
\subsection{Method \code{compute_objective()}}{
Evaluate the three-term objective at the current solution \verb{$X_hat}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ADMMSolver$compute_objective()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
Named numeric vector with elements \code{data}, \code{temporal},
\code{spatial}, and \code{total}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ADMMSolver-print"></a>}}
\if{latex}{\out{\hypertarget{method-ADMMSolver-print}{}}}
\subsection{Method \code{print()}}{
Print a concise solver summary.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ADMMSolver$print(...)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{...}}{Unused; accepted for compatibility with the generic
\code{print()} method.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-ADMMSolver-clone"></a>}}
\if{latex}{\out{\hypertarget{method-ADMMSolver-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{ADMMSolver$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
