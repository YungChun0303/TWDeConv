% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admm_solver.R
\name{solve_deconv}
\alias{solve_deconv}
\title{Solve the Spatio-Temporal Deconvolution Problem}
\usage{
solve_deconv(
  Y,
  M,
  W = NULL,
  D,
  Ls,
  lambda_t,
  lambda_s,
  rho = 1,
  ridge = 1e-08,
  max_iter = 1000L,
  tol_abs = 1e-04,
  tol_rel = 0.001,
  verbose = FALSE,
  return_solver = FALSE
)
}
\arguments{
\item{Y}{n  x  S numeric matrix of observed smoothed data.}

\item{M}{S  x  T convolution matrix (from
\code{SyntheticDataGenerator$M} or \code{\link[=build_temporal_penalty]{build_temporal_penalty()}}).}

\item{W}{(nS)  x  (nS) sparse diagonal precision matrix from
\code{\link[=build_precision_weights]{build_precision_weights()}}.  Pass \code{NULL} for ordinary (unweighted)
least squares; pass a positive scalar for homoscedastic WLS.}

\item{D}{(T-k)  x  T temporal difference matrix from
\code{\link[=build_temporal_penalty]{build_temporal_penalty()}}.}

\item{Ls}{n  x  n spatial graph Laplacian from
\code{\link[=build_spatial_laplacian]{build_spatial_laplacian()}}.}

\item{lambda_t}{Non-negative scalar.  Temporal regularisation
strength (\eqn{\lambda_t}).}

\item{lambda_s}{Non-negative scalar.  Spatial regularisation
strength (\eqn{\lambda_s}).}

\item{rho}{Positive scalar.  ADMM augmented-Lagrangian penalty
parameter (default \code{1}).  A useful heuristic is
\code{rho = sqrt(lambda_t * lambda_s)} or to set it near the expected
signal-to-noise ratio.}

\item{ridge}{Small positive ridge added to the system matrix
diagonal to guarantee strict positive definiteness (default \code{1e-8}).}

\item{max_iter}{Maximum ADMM iterations (default \code{1000}).}

\item{tol_abs}{Absolute convergence tolerance (default \code{1e-4}).}

\item{tol_rel}{Relative convergence tolerance (default \code{1e-3}).}

\item{verbose}{Logical.  Print convergence progress (default \code{FALSE}).}

\item{return_solver}{Logical.  If \code{TRUE}, return the full \link{ADMMSolver}
object (which carries \verb{$X_hat}, \verb{$history}, \verb{$A_sys}, etc.) instead
of just \verb{$X_hat} (default \code{FALSE}).}
}
\value{
\itemize{
\item If \code{return_solver = FALSE} (default): an n  x  T numeric matrix
\eqn{\hat{X}} with row names from \code{Y} and column names
\code{year_1}, ..., \code{year_T}.
\item If \code{return_solver = TRUE}: the \link{ADMMSolver} R6 object.
}
}
\description{
Recovers the latent annual signal matrix \eqn{\hat{X}} from observed
multi-year moving-average data \eqn{Y} by solving the Kronecker-structured
generalised lasso

\deqn{\min_X \;
  \tfrac{1}{2} \| W^{1/2} \bigl(\operatorname{vec}(Y)
    - (I_n \otimes M)\operatorname{vec}(X^\top)\bigr) \|_2^2
  + \lambda_t \|(I_n \otimes D)\operatorname{vec}(X^\top)\|_1
  + \lambda_s \|(L_s \otimes I_T)\operatorname{vec}(X^\top)\|_1}

via ADMM with a precomputed sparse Cholesky factorisation.  See
\link{ADMMSolver} for full algorithmic details.
}
\examples{
\dontrun{
# -- Simulate ground-truth data --------------------------------
gen <- SyntheticDataGenerator$new(n_row = 8, n_col = 8,
                                  T_years = 20, seed = 42L)
gen$simulate()

# -- Build penalty operators ----------------------------------
D  <- build_temporal_penalty(gen$params$T_years, k = 2)
Ls <- build_spatial_laplacian(gen$grid, queen = FALSE)
W  <- build_precision_weights(matrix(0.5, nrow = 64, ncol = 16))

# -- Deconvolve -----------------------------------------------
X_hat <- solve_deconv(
  Y = gen$Y, M = gen$M, W = W, D = D, Ls = Ls,
  lambda_t = 0.1, lambda_s = 0.05,
  verbose  = TRUE
)
dim(X_hat)  # 64 x 20

# -- Recovery quality -----------------------------------------
cat("RMSE:", sqrt(mean((X_hat - gen$X)^2)), "\n")
cat("Baseline (Y vs X):",
    sqrt(mean((gen$Y - gen$X[, 1:ncol(gen$Y)])^2)), "\n")

# -- Return full solver object for diagnostics ----------------
solver <- solve_deconv(
  Y = gen$Y, M = gen$M, W = W, D = D, Ls = Ls,
  lambda_t = 0.1, lambda_s = 0.05,
  return_solver = TRUE
)
solver$compute_objective()
plot(solver$history$r_primal, type = "l", log = "y",
     ylab = "Primal residual", xlab = "Iteration")
}

}
\seealso{
\link{ADMMSolver}, \code{\link[=build_temporal_penalty]{build_temporal_penalty()}},
\code{\link[=build_spatial_laplacian]{build_spatial_laplacian()}}, \code{\link[=build_precision_weights]{build_precision_weights()}},
\link{SyntheticDataGenerator}
}
